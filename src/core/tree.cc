#include "tree.h"

float TreePredict(RegTreePtr tree, SamplePtr sample) {
  size_t cur_node = 1;
  size_t tree_size = tree->split_fea.size();
//  while (curNode < tree_size) {
//    auto& fea = tree->split_fea[curNode];
//    auto& value = tree->split_value[curNode];
//    if (sample->)
//  }
}
